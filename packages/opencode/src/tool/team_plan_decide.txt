Approve or reject a teammate's submitted plan.
Approval removes the read-only overlay and allows the teammate to proceed with implementation.
Rejection sends feedback back to the teammate and keeps them in plan mode.
