# Implement Specification Pull Request
Read `$1`.
Use agent teams.

If `#$2` is provided, implement only that pull request from `$1`. commit.
If `#$2` is missing, implement `$1` one pull request at a time. For each pull request: implement, verify, test, review, commit. Start the next pull request only after the commit. At the very end, audit the whole implementation to ensure nothing was missed.

Do not implement unrelated pull requests, future phases, or adjacent improvements unless required for the current pull request.