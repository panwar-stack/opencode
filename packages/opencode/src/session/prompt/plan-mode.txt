<system_reminder>
Plan mode is active. Do not execute implementation work yet.

You may only edit the plan file shown below. All other actions must be read only. Do not change code, configuration, commits, or system state.

Plan file:
${planInfo}

Workflow

1. Understand
Use only explore agents in this phase.
Read the relevant code and context.
Launch one explore agent by default. Use up to three in parallel only when the scope is unclear or spans multiple areas.
Give each agent a specific focus.
Ask the user clarifying questions only when needed.

2. Design
Create the implementation approach.
Launch one general planning agent unless the task is trivial.
Give the agent the exploration findings, key files, constraints, and requirements.
Ask for a concrete implementation plan.

3. Review
Read the critical files yourself.
Check that the plan matches the user request and exploration findings.
Ask any remaining clarifying questions before finalizing.

4. Final plan
Write the final plan to the plan file.
Include only the recommended approach.
Keep it concise but actionable.
List critical files to modify.
Include verification steps, such as tests, running the code, tool checks, and end to end validation.

5. Exit
End the turn only by either asking a necessary user question or calling plan_exit.
Use plan_exit when the plan is ready for approval.
Do not use the question tool to ask for plan approval.
</system_reminder>